<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <!-- ==================== FORM VIEW ==================== -->
    <record id="view_branch_transfer_request_form" model="ir.ui.view">
        <field name="name">branch.transfer.request.form</field>
        <field name="model">branch.transfer.request</field>
        <field name="arch" type="xml">
            <form string="Branch Transfer Request">
                <header>
                    <button name="action_confirm"
                            string="Confirm &amp; Send Request"
                            type="object"
                            class="btn-primary"
                            invisible="state != 'draft'"
                            confirm="This will send a notification to the destination branch. Continue?"/>
                    <button name="action_approve"
                            string="Approve"
                            type="object"
                            class="btn-success"
                            invisible="state != 'confirmed' or not is_dest_incharge or not has_approved_lines"/>
                    <button name="action_done"
                            string="Receive"
                            type="object"
                            class="btn-primary"
                            invisible="state != 'approved' or not is_source_incharge or not has_done_lines"
                            confirm="This will auto-create and validate the receipt for your branch. Continue?"/>
                    <button name="action_cancel"
                            string="Cancel"
                            type="object"
                            class="btn-danger"
                            invisible="state in ('done', 'cancelled') or not is_dest_incharge"/>
                    <button name="action_reset_draft"
                            string="Reset to Draft"
                            type="object"
                            invisible="state != 'cancelled'"/>
                    <button name="action_request_return"
                            string="Report Damaged"
                            type="object"
                            class="btn-warning"
                            invisible="state != 'done' or not is_source_incharge"/>
                    <button name="action_create_return_receipt"
                            string="Create Return Receipt"
                            type="object"
                            class="btn-primary"
                            invisible="return_state != 'requested' or not is_dest_incharge"/>
                    <field name="state" widget="statusbar"
                           statusbar_visible="draft,confirmed,approved,done"/>
                </header>

                <sheet>
                    <!-- Hidden fields for smart button visibility conditions -->
                    <field name="delivery_id" invisible="1"/>
                    <field name="receipt_id" invisible="1"/>
                    <field name="is_source_incharge" invisible="1"/>
                    <field name="is_dest_incharge" invisible="1"/>
                    <field name="has_approved_lines" invisible="1"/>
                    <field name="has_done_lines" invisible="1"/>

                    <div class="oe_title">
                        <h1>
                            <field name="name" readonly="1"/>
                        </h1>
                    </div>

                    <!-- Smart Buttons -->
                    <div name="button_box" class="oe_button_box">
                        <button name="action_view_delivery"
                                type="object"
                                class="oe_stat_button"
                                icon="fa-truck"
                                invisible="not delivery_id or not is_dest_incharge">
                            <span class="o_stat_text">Delivery</span>
                        </button>
                        <button name="action_view_receipt"
                                type="object"
                                class="oe_stat_button"
                                icon="fa-sign-in"
                                invisible="not receipt_id or not is_source_incharge">
                            <span class="o_stat_text">Receipt</span>
                        </button>
                    </div>

                    <group>
                        <group string="Source Warehouse (From)">
                            <field name="source_branch_id"
                                   readonly="state != 'draft'"/>
                            <field name="source_warehouse_id"
                                   readonly="state != 'draft'"/>
                        </group>
                        <group string="Destination Warehouse (To)">
                            <field name="dest_warehouse_id"
                                   readonly="state != 'draft'"/>
                        </group>
                    </group>

                    <group>
                        <group string="Request Info">
                            <field name="date_request" readonly="state != 'draft'"/>
                            <field name="requested_by" readonly="1"/>
                        </group>
                        <group string="Approval Info">
                            <field name="approved_by" readonly="1"
                                   invisible="state not in ('approved', 'done')"/>
                            <field name="date_approved" readonly="1"
                                   invisible="state not in ('approved', 'done')"/>
                            <field name="date_done" readonly="1"
                                   invisible="state != 'done'"/>
                            <field name="delivery_name" invisible="not delivery_name"/>
                            <field name="receipt_name" invisible="not receipt_name"/>
                        </group>
                    </group>

                    <notebook>
                        <page string="Products" name="products">
                            <field name="line_ids"
                                   readonly="state not in ('draft', 'confirmed', 'approved')"
                                   widget="one2many">
                                <list editable="bottom">
                                    <field name="product_id" readonly="parent.state != 'draft'" required="1"/>
                                    <field name="product_uom_id" readonly="parent.state != 'draft'"/>
                                    <field name="qty_requested" readonly="parent.state != 'draft'" required="1"/>
                                    <field name="qty_approved"
                                           readonly="parent.state != 'confirmed' or not parent.is_dest_incharge"
                                           column_invisible="parent.state not in ('confirmed', 'approved', 'done')"/>
                                    <field name="is_approved"
                                           string="Approve"
                                           readonly="parent.state != 'confirmed' or not parent.is_dest_incharge"
                                           column_invisible="parent.state not in ('confirmed', 'approved', 'done')"/>
                                    <field name="qty_done"
                                           readonly="parent.state != 'approved' or not parent.is_source_incharge"
                                           column_invisible="parent.state not in ('approved', 'done')"/>
                                    <field name="is_done"
                                           string="Done"
                                           readonly="parent.state != 'approved' or not parent.is_source_incharge"
                                           column_invisible="parent.state not in ('approved', 'done')"/>
                                    <field name="notes"/>
                                </list>
                            </field>
                        </page>
                        <page string="Notes" name="notes_page">
                            <field name="notes"
                                   placeholder="Add any special instructions or notes..."
                                   readonly="state not in ('draft',)"/>
                        </page>
                        <page string="Return Notes" name="return_notes_page">
                            <field name="return_reason"
                                   placeholder="Enter reason for return/damage..."
                                   readonly="return_state != 'none'"/>
                        </page>
                    </notebook>
                </sheet>

                <chatter/>
            </form>
        </field>
    </record>

    <!-- ==================== TREE/LIST VIEW ==================== -->
    <record id="view_branch_transfer_request_tree" model="ir.ui.view">
        <field name="name">branch.transfer.request.tree</field>
        <field name="model">branch.transfer.request</field>
        <field name="arch" type="xml">
            <list string="Branch Transfer Requests"
                  decoration-info="state == 'draft'"
                  decoration-warning="state == 'confirmed'"
                  decoration-success="state in ('approved', 'done')"
                  decoration-muted="state == 'cancelled'">
                <field name="name"/>
                <field name="date_request"/>
                <field name="source_branch_id"/>
                <field name="source_warehouse_id"/>
                <field name="dest_warehouse_id"/>
                <field name="requested_by"/>
                <field name="line_count" string="# Products"/>
                <field name="state" widget="badge"
                       decoration-info="state == 'draft'"
                       decoration-warning="state == 'confirmed'"
                       decoration-success="state in ('approved', 'done')"
                       decoration-danger="state == 'cancelled'"/>
            </list>
        </field>
    </record>

    <!-- ==================== SEARCH VIEW ==================== -->
    <record id="view_branch_transfer_request_search" model="ir.ui.view">
        <field name="name">branch.transfer.request.search</field>
        <field name="model">branch.transfer.request</field>
        <field name="arch" type="xml">
            <search string="Search Transfer Requests">
                <field name="name" string="Reference"/>
                <field name="source_branch_id" string="Source Branch"/>
                <field name="requested_by"/>
                <filter name="filter_draft" string="Draft" domain="[('state','=','draft')]"/>
                <filter name="filter_confirmed" string="Confirmed" domain="[('state','=','confirmed')]"/>
                <filter name="filter_approved" string="Approved" domain="[('state','=','approved')]"/>
                <filter name="filter_done" string="Done" domain="[('state','=','done')]"/>
                <filter name="filter_cancelled" string="Cancelled" domain="[('state','=','cancelled')]"/>
                <separator/>
                <filter name="filter_my_requests" string="My Requests"
                        domain="[('requested_by','=',uid)]"/>
                <group expand="0" string="Group By">
                    <filter name="group_source" string="Source Branch"
                            context="{'group_by': 'source_branch_id'}"/>
                    <filter name="group_state" string="Status"
                            context="{'group_by': 'state'}"/>
                    <filter name="group_date" string="Request Date"
                            context="{'group_by': 'date_request:month'}"/>
                </group>
            </search>
        </field>
    </record>

    <!-- ==================== ACTION ==================== -->
    <record id="action_branch_transfer_request" model="ir.actions.act_window">
        <field name="name">Transfer Requests</field>
        <field name="res_model">branch.transfer.request</field>
        <field name="view_mode">list,form</field>
        <field name="search_view_id" ref="view_branch_transfer_request_search"/>
        <field name="help" type="html">
            <p class="o_view_nocontent_smiling_face">
                Create your first Branch Transfer Request!
            </p>
            <p>
                Send product transfer requests to other branches.
                The destination branch will be notified automatically.
            </p>
        </field>
    </record>

</odoo>